[pytest]
pythonpath = .
testpaths = tests
norecursedirs = scratch .git .venv venv
filterwarnings =
    ignore::DeprecationWarning
# Production Neon is blocked unless TEST_DATABASE_URL is set.
# See tests/conftest.py and docker-compose.test.yml.
